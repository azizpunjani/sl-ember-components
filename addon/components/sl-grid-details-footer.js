import Ember from 'ember';
import layout from '../templates/components/sl-grid-details-footer';

export default Ember.Component.extend({
    layout: layout,
    tagName: 'footer',
    classNames: 'panel-footer'
});
